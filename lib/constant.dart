import 'package:flutter/material.dart';

// Default colors
const dfBGColor = Colors.black;
const dfFontColor = Colors.white;

//Default under working
const dfColor = Color.fromRGBO(24, 25, 29, 1);

// Default font Family
const String dfFontFamily = 'Raleway';

// Default margin
const double dfMargin = 10.0;

// App Bar font size

const double dfAppFont = 26;

// Default Wifi Name Font size
const double dfWifiNameFont = 21;

// Default font size
const double dfFSize = 22.0;
const double dfFSmSize = 10.0;

//Border Radius
const double dfRadius = 10.0;

const colorLoder = Color.fromRGBO(36, 97, 177, 1);

const colorbackground = Color.fromRGBO(36, 97, 177, 1);

const dfListColor = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      Color.fromRGBO(135, 135, 135, 1),
      Color.fromRGBO(68, 68, 68, 1),
    ]);
const dfListBlue = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      Color.fromRGBO(33, 97, 180, 1),
      Color.fromRGBO(15, 45, 85, 1),
    ]);
final dfListGreen = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      Colors.green.shade300,
      Colors.green.shade800,
    ]);


// const dfLinearPlayColor = LinearGradient(
//   begin: Alignment.topLeft,
//   end: Alignment.topRight,
//   stops: [0.1, 0.4, 0.6, 1],
//   colors: [
//     // Color.fromRGBO(85, 173, 178, 1),

//     Color.fromRGBO(61, 16, 29, 1),
//     Color.fromRGBO(24, 25, 29, 1),
//     Color.fromRGBO(24, 25, 29, 1),
//     Color.fromRGBO(61, 16, 29, 1)
//   ],
// );
